"""
pipeline package — data export and ingestion for MDT AI Copilot.

Modules:
  db           — database init and schema (SQLite)
  log_parser   — parse gNB stdout MDT log lines
  synthetic_gen — generate synthetic UE RSRP samples with injected anomalies
"""
